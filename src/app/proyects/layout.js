import "../globals.css";

export const metadata = {
    title: "Jeff-Proyects",
    description: "portfolio Jeff development projects",
};

export default function ProyectLayout({ children }) {
    return (
        <>
            {children}
        </>

    );
}
